# 03. HTTP 메시지

## 3.1 메시지의 흐름

- HTTP 통신은 메시지를 주고받는 형태로 이루어진다.
- 메시지는 텍스트 기반의 메타 정보(시작줄, 헤더)와 선택적인 데이터(본문)로 구성된다.

### 3.1.1 인바운드와 아웃바운드
<img width="623" height="280" alt="img" src="https://github.com/user-attachments/assets/e9903e84-7087-4ea0-8843-b3051c6d8f0c" />

- **인바운드**: 클라이언트에서 원 서버를 향해 이동하는 것
- **아웃바운드**: 서버에서 모든 처리를 마친 뒤 사용자 에이전트로 돌아오는 것

### 3.1.2 다운스트림
<img width="598" height="507" alt="img_1" src="https://github.com/user-attachments/assets/bbcfeb1b-a398-4b9f-81a6-0dbf4f8ec665" />

- HTTP에서 메시지는 언제나 다운스트림 방향으로 흐른다.
- 요청 메시지든 응답 메시지든 상관없이 동일하게 적용된다.

---

## 3.2 메시지의 각 부분
<img width="626" height="200" alt="img_2" src="https://github.com/user-attachments/assets/7a6be49e-9662-4d7d-9e25-f18b114ba22d" />

- HTTP 메시지는 **시작줄**, **헤더 블록**, **본문** 세 부분으로 나뉜다.
- 시작줄은 어떤 메시지인지 알려주고, 헤더는 속성을, 본문은 실제 데이터를 담는다.
- 각 줄은 CRLF(캐리지 리턴 + 개행)로 끝나며, 헤더와 본문 사이에는 빈 줄이 온다.

### 3.2.1 메시지 문법

HTTP 메시지는 요청 메시지와 응답 메시지로 구분된다.

**요청 메시지:**
```
<메서드> <요청 URL> <버전>
<헤더>

<엔티티 본문>
```

**응답 메시지:**
```
<버전> <상태 코드> <사유 구절>
<헤더>

<엔티티 본문>
```

> 💡 **HTTP/2에서는 형식이 다르다:**
>
> HTTP/2는 텍스트가 아닌 **바이너리 프레임** 구조를 사용한다.
>
> ```
> HTTP/1.1 = 편지 (한 번에 하나씩 순서대로)
> HTTP/2   = 택배 박스들 (여러 개를 동시에, Stream ID로 구분)
> ```
>
> **프레임 구조:**
> ```
> ┌─────────────────────────────────────┐
> │ Frame Header (9 bytes)              │
> │ - Length (3 bytes)                  │
> │ - Type (1 byte)                     │
> │ - Flags (1 byte)                    │
> │ - Stream ID (4 bytes)               │
> ├─────────────────────────────────────┤
> │ Frame Payload (가변)                 │
> └─────────────────────────────────────┘
> ```

### 3.2.2 시작줄

시작줄은 메시지의 첫 번째 줄로, 요청과 응답에서 역할이 다르다.

**요청줄(Request Line):**
- 서버에게 무슨 동작을 수행해야 하는지 알린다.
- 메서드, 요청 URL, HTTP 버전이 공백으로 구분되어 나열된다.

**응답줄(Status Line):**
- 처리 결과가 어떻게 되었는지 클라이언트에게 알린다.
- HTTP 버전, 상태 코드, 사유 구절로 구성된다.

### 메서드

서버에게 수행을 요청하는 동작이다.

| 메서드 | 설명 | 본문 |
|--------|------|------|
| GET | 서버에서 리소스를 가져온다 | 없음 |
| HEAD | GET과 같지만 헤더만 반환 | 없음 |
| POST | 서버가 처리해야 할 데이터를 전송 | 있음 |
| PUT | 요청 URL에 리소스를 저장 | 있음 |
| DELETE | 서버에서 리소스를 제거 | 없음 |
| TRACE | 프락시 등을 거쳐 서버에 도달한 요청을 확인 | 없음 |
| OPTIONS | 서버가 지원하는 메서드를 질의 | 없음 |

### 요청 URL

- 요청 대상 리소스를 지정한다.
- 완전한 URL이나 경로 형태로 전달된다.
- Host 헤더와 함께 사용되어 실제 서버와 리소스를 식별한다.

### 버전

- 사용 중인 HTTP 버전을 나타낸다.
- `HTTP/<메이저>.<마이너>` 형식이다.
- 버전 비교 시 주의: HTTP/2.22와 HTTP/2.3을 비교할 때 분수처럼 계산하면 안 된다.

### 상태 코드

요청이 어떻게 처리되었는지 세 자리 숫자로 알려준다.

| 범위 | 분류 | 의미 |
|------|------|------|
| 1xx | 정보 | 요청을 받았고 처리 중임을 알림 |
| 2xx | 성공 | 요청이 정상적으로 처리됨 |
| 3xx | 리다이렉션 | 요청을 완료하려면 추가 동작 필요 |
| 4xx | 클라이언트 오류 | 요청에 문제가 있음 |
| 5xx | 서버 오류 | 서버에서 요청 처리 중 오류 발생 |

### 사유 구절

- 상태 코드와 짝지어 사람이 읽을 수 있는 설명을 제공한다.
- 예: `200 OK`, `404 Not Found`
- 프로그램 로직에는 영향을 주지 않으며, 같은 상태 코드면 동일하게 처리해야 한다.

### 3.2.3 헤더

- 시작줄 다음에 위치하며, 메시지에 대한 부가 정보를 제공한다.
- `이름: 값` 형식의 쌍으로 이루어진다.
- 헤더 블록의 끝은 빈 줄(CRLF)로 표시한다.

**헤더 분류:**
- **일반 헤더**: 요청/응답 모두에 사용 (예: Date, Connection)
- **요청 헤더**: 요청에 대한 정보 (예: Host, User-Agent)
- **응답 헤더**: 응답에 대한 정보 (예: Server, Set-Cookie)
- **엔티티 헤더**: 본문에 대한 정보 (예: Content-Type, Content-Length)
- **확장 헤더**: 표준에 정의되지 않은 커스텀 헤더

### 3.2.4 엔티티 본문

- 메시지가 실어 나르는 실제 데이터 영역이다.
- 이미지, HTML, JSON 등 다양한 형태의 데이터가 올 수 있다.
- 본문이 없는 메시지도 많다 (예: GET 요청, 204 응답).

### 3.2.5 버전 0.9 메시지

- HTTP의 가장 초기 버전이다.
- 요청: 메서드와 URL만 포함 (헤더 없음)
- 응답: 본문만 포함 (상태 코드, 헤더 없음)
- 현재는 거의 사용되지 않지만 일부 서버에서 호환성 유지를 위해 지원한다.

---

## 3.3 메서드

### 3.3.1 안전한 메서드(Safe Method)

- 서버의 상태를 변경하지 않는 읽기 전용 메서드다.
- GET과 HEAD가 대표적이다.

### 3.3.2 GET

- HTTP에서 가장 많이 사용되는 메서드다.
- 지정한 URL의 리소스를 가져온다.
- HTTP/1.1을 지원하는 서버라면 반드시 구현해야 한다.

### 3.3.3 HEAD

- GET과 동일하게 동작하지만 응답 본문을 포함하지 않는다.
- 활용 예:
  - 리소스가 존재하는지 확인
  - Content-Type 등 메타 정보 조회
  - Last-Modified로 변경 여부 확인

### 3.3.4 PUT

- 요청 URL로 지정한 위치에 리소스를 생성하거나 대체한다.
- 리소스 전체를 교체하는 의미로 사용된다.
- 보통 인증이 필요하다.

### 3.3.5 POST

- 서버로 데이터를 전송하여 처리를 요청한다.
- HTML 폼 제출, API 호출 등에 널리 쓰인다.

> 💡 POST는 서버에 데이터를 보내기 위해, PUT은 리소스에 데이터를 입력하기 위해 사용한다. 

### 3.3.6 TRACE

- 클라이언트가 보낸 요청이 서버에 어떻게 도착하는지 추적한다.
- 프락시나 게이트웨이를 거치며 요청이 변경될 수 있는데, 이를 진단하는 용도다.
- 보안 취약점으로 인해 대부분의 서버에서 비활성화되어 있다.

### 3.3.7 OPTIONS

- 서버나 특정 리소스가 어떤 메서드를 지원하는지 확인한다.
- 응답의 Allow 헤더에 지원 메서드 목록이 담긴다.
- CORS 사전 요청(Preflight)에서도 사용된다.

### 3.3.8 DELETE

- 지정한 URL의 리소스 삭제를 요청한다.
- 실제로 삭제될지는 서버 구현에 따라 다르다.

### 3.3.9 확장 메서드

- HTTP는 새로운 메서드를 추가할 수 있도록 설계되었다.
- WebDAV의 LOCK, COPY, MOVE 등이 대표적인 확장 메서드다.
- 프락시는 관용적으로 알지 못하는 메서드라도 다음 홉으로 전달해야 한다.

---

## 3.4 상태 코드

### 3.4.1 100-199: 정보성 상태 코드

| 코드 | 사유 구절 | 의미 |
|------|-----------|------|
| 100 | Continue | 요청 시작 부분을 받았으며 계속 진행해도 좋음 |
| 101 | Switching Protocols | 프로토콜 전환 수락 (예: WebSocket) |

> 💡 **100 Continue 동작 흐름:**
> - 클라이언트: 큰 본문을 보내기 전 `Expect: 100-continue` 헤더로 서버 상태 확인
> - 서버: 준비되면 100 응답, 문제가 있으면 에러 코드 반환
> - 일정 시간 응답이 없으면 클라이언트는 본문을 그냥 전송

### 3.4.2 200-299: 성공 상태 코드

| 코드 | 사유 구절 | 의미 |
|------|-----------|------|
| 200 | OK | 요청 성공, 본문에 요청된 리소스 포함 |
| 201 | Created | 새 리소스 생성 완료 (PUT 등) |
| 202 | Accepted | 요청 접수됨, 처리 완료는 보장하지 않음 |
| 204 | No Content | 성공했으나 돌려줄 본문 없음 |
| 206 | Partial Content | Range 요청에 대한 부분 응답 |

### 3.4.3 300-399: 리다이렉션 상태 코드

| 코드 | 사유 구절 | 의미 |
|------|-----------|------|
| 301 | Moved Permanently | 리소스가 영구적으로 이동, Location 헤더에 새 URL |
| 302 | Found | 임시 이동, 원래 URL을 계속 사용해야 함 |
| 303 | See Other | 다른 URL 참조 권장 (POST 결과 페이지 등) |
| 304 | Not Modified | 조건부 요청에서 변경 없음, 캐시 사용 가능 |
| 307 | Temporary Redirect | 임시 이동, 요청 메서드 유지 |

### 3.4.4 400-499: 클라이언트 에러 상태 코드

| 코드 | 사유 구절 | 의미 |
|------|-----------|------|
| 400 | Bad Request | 요청 형식이 잘못됨 |
| 401 | Unauthorized | 인증 필요 |
| 403 | Forbidden | 서버가 요청 거부 |
| 404 | Not Found | 요청한 리소스가 없음 |
| 405 | Method Not Allowed | 해당 URL에서 지원하지 않는 메서드 |
| 408 | Request Timeout | 클라이언트 요청 완료 대기 시간 초과 |
| 409 | Conflict | 요청이 리소스 상태와 충돌 |

### 3.4.5 500-599: 서버 에러 상태 코드

| 코드 | 사유 구절 | 의미 |
|------|-----------|------|
| 500 | Internal Server Error | 서버 내부 오류 |
| 501 | Not Implemented | 요청한 기능을 서버가 지원하지 않음 |
| 502 | Bad Gateway | 게이트웨이가 상위 서버로부터 잘못된 응답 수신 |
| 503 | Service Unavailable | 서버가 일시적으로 요청 처리 불가 |
| 504 | Gateway Timeout | 게이트웨이가 상위 서버 응답 대기 시간 초과 |

---

## 3.5 헤더

### 3.5.1 일반 헤더

요청과 응답 모두에서 사용되는 범용 헤더다.

| 헤더 | 설명 |
|------|------|
| Date | 메시지 생성 일시 |
| Connection | 커넥션 관리 옵션 |
| Cache-Control | 캐싱 지시자 |
| Transfer-Encoding | 전송 인코딩 방식 |

### 3.5.2 요청 헤더

클라이언트가 서버에게 자신의 정보나 선호도를 알린다.

**기본 요청 헤더:**

| 헤더 | 설명 |
|------|------|
| Host | 요청 대상 서버의 호스트명과 포트 |
| User-Agent | 클라이언트 애플리케이션 정보 |
| Referer | 현재 요청을 보내게 된 이전 페이지 URL |

**Accept 관련 헤더:**

| 헤더 | 설명 |
|------|------|
| Accept | 받고자 하는 미디어 타입 |
| Accept-Language | 선호하는 언어 |
| Accept-Encoding | 지원하는 인코딩 방식 |

**조건부 요청 헤더:**

| 헤더 | 설명 |
|------|------|
| If-Modified-Since | 지정 시간 이후 변경된 경우에만 응답 |
| If-None-Match | ETag가 일치하지 않을 때만 응답 |

### 3.5.3 응답 헤더

서버가 클라이언트에게 부가 정보를 전달한다.

| 헤더 | 설명 |
|------|------|
| Server | 서버 소프트웨어 정보 |
| Location | 리다이렉션 대상 URL |
| Set-Cookie | 클라이언트에 쿠키 설정 |
| WWW-Authenticate | 인증 방식 안내 |

### 3.5.4 엔티티 헤더

본문(엔티티)에 대한 정보를 담는다.

**콘텐츠 헤더:**

| 헤더 | 설명 |
|------|------|
| Content-Type | 본문의 미디어 타입 |
| Content-Length | 본문 크기(바이트) |
| Content-Encoding | 본문 인코딩 방식 |
| Content-Language | 본문의 언어 |

**캐싱 헤더:**

| 헤더 | 설명 |
|------|------|
| ETag | 리소스의 고유 식별자 |
| Last-Modified | 리소스 마지막 수정 시간 |
| Expires | 응답 만료 시간 |

> 💡 **HTTP/1.1 vs HTTP/2 비교:**
>
> | 항목 | HTTP/1.1 | HTTP/2 |
> |------|----------|--------|
> | 형식 | 텍스트 | 바이너리 |
> | 헤더 | 평문 (매번 중복 전송) | HPACK 압축 |
> | 요청 | 순차 처리 (HOL 발생) | 멀티플렉싱 |
> | 단위 | 메시지 | 프레임 → 스트림 |

> 💡 **주요 프레임 타입:**
>
> | 타입 | 역할 |
> |------|------|
> | HEADERS | 요청/응답 헤더 전송 |
> | DATA | 본문 데이터 전송 |
> | SETTINGS | 연결 설정 협상 |
> | WINDOW_UPDATE | 흐름 제어 |

> 💡 **Stream과 멀티플렉싱:**
> ```
> ┌───────────────────────────────────────────────┐
> │              하나의 TCP 연결                    │
> │                                               │
> │  Stream 1: 요청A → [HEADERS][DATA]            │
> │  Stream 3: 요청B → [HEADERS][DATA][DATA]      │
> │  Stream 5: 요청C → [HEADERS]                  │
> │                                               │
> │  → 프레임들이 섞여서 동시에 전송되지만,           │
> │    Stream ID로 어떤 요청인지 구분               │
> └───────────────────────────────────────────────┘
> ```
